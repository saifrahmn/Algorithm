#include<std/bitsc++.h>
#include<iostream>
//finding the heap
int heapify(arr[]){
    
}

int[] sort(int[] arr){
    int heap[]=heapify(arr[]);
}
//main function
int main(){
    int arr[];
    int ele;
    std::cout<<"Enter the number of elements";
    std::cin>>ele;
    for(int i=0;i<ele;i++){
        std::cin>>ele;
    }
    sort(arr);
}