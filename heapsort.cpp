#include<bits/stdc++.h>
#include<iostream>
//finding the heap
int heapify(int[] arr){
    int top;
    for(int i=0;i<ele;i++){
        if(arr[])
    }
    
}

int sort(int[] arr,ele){
    int heap[]=heapify(arr,ele);
}
//main function
int main(){
    int arr[];
    int ele;
    std::cout<<"Enter the number of elements";
    std::cin>>ele;
    for(int i=0;i<ele;i++){
        std::cin>>ele;
    }
    std::sort(arr,ele);
}